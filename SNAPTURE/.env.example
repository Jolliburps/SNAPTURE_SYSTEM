# Optional explicit API URL. In Expo development, the app automatically uses
# the host that served the bundle (LAN IP for phones, browser host for web).
# Keep this as a fallback for a standalone/native build or if host discovery
# is unavailable. Update the IP when the computer changes networks.
EXPO_PUBLIC_API_URL=http://192.168.1.171:8000
